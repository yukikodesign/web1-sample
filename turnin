# git add --all
# git commit -m "turning in"
pushcommit=$(git push origin gh-pages)
if [[ $? != 0 ]]; then
  echo -e "******** \n   Turn in failed!\n ******** "
elif [[ pushcommit ]]; then
  echo pushcommit
  echo -e " ----- \n Successfully Turned in!\n ------ "
fi
