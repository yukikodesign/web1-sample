git add --all
git commit -m "turning in"
pushcommit = $(git push origin gh-pages)
if [[ $? != 0 ]] then
  echo "turn in failed"
else if [[ pushcommit ]]
  echo pushcommit
  echo " ----- Successfully Turned in! ------ "
fi