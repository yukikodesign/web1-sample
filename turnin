clear
git add --all
git commit -m "turning in"
pushcommit=$(git push origin gh-pages)
if [[ $? != 0 ]]; then
  echo -e "\n\n******** \n   Turn in failed!\n ******** "
else
  echo -e "\n\n ----- \n Successfully Turned in!\n ------ "
fi
